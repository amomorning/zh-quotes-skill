# Typography Validation Checklist

Before responding:

- Is this prose or syntax?
- Does this format require ASCII symbols?
- Is there nested language context?
- Are Chinese quotes accidentally used in code?
- Are parser-required symbols preserved?
- Are Chinese quotations inside strings correctly formatted?
- Does Typst still parse correctly?
- Does JSON/YAML remain valid?
